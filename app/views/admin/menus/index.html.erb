<h1>Listing admin_menus</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Introduction</th>
    <th>Path</th>
    <th>Admin</th>
    <th>Disable</th>
    <th>Position</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_menus.each do |admin_menu| %>
  <tr>
    <td><%= admin_menu.title %></td>
    <td><%= admin_menu.introduction %></td>
    <td><%= admin_menu.path %></td>
    <td><%= admin_menu.admin %></td>
    <td><%= admin_menu.disable %></td>
    <td><%= admin_menu.position %></td>
    <td><%= link_to 'Show', admin_menu %></td>
    <td><%= link_to 'Edit', edit_admin_menu_path(admin_menu) %></td>
    <td><%= link_to 'Destroy', admin_menu, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu', new_admin_menu_path %>
