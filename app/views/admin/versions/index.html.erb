<table>
  <tr>
    <th>Name</th>
    <th>Used</th>
    <th>Current</th>
    <th></th>
    <th></th>
  </tr>

<% @admin_versions.each do |admin_version| %>
  <tr>
    <td><%= admin_version.name %></td>
    <td><%= admin_version.used %></td>
    <td><%= admin_version.current %></td>
    <td><%= link_to 'Edit', edit_admin_version_path(admin_version) %></td>
    <td><%= link_to 'Destroy', admin_version, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Version', new_admin_version_path %>
