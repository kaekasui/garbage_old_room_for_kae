<%= form_for(@admin_access_count) do |f| %>
  <% if @admin_access_count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_access_count.errors.count, "error") %> prohibited this admin_access_count from being saved:</h2>

      <ul>
      <% @admin_access_count.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :access_at %><br />
    <%= f.date_select :access_at %>
  </div>
  <div class="field">
    <%= f.label :cnt %><br />
    <%= f.number_field :cnt %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
