<p id="notice"><%= notice %></p>

<table class="table">
<%= render :partial => "contents1", :locals => {:blog => @blog} %>
  <tr>
    <td colspan="2"><%= to_br(sanitize_blog(@blog.id, false)).html_safe %></td>   </tr>
  <tr>
    <td><%= link_to "comments(#{@blog.comment_counts})", blog_path(@blog.id) + ".html#comment" %>
<%#= link_to image_tag(get_comment_icon_image) + "comments(#{@blog.comment_counts})", blog_path(@blog.id) + '.html#comment' %></td>  </tr>
</table>

<%= form_for :blog_comment, html: {class: "form-horizontal"}, :url => confirm_blog_blog_comments_path(blog_id: @blog.id) do |f| %>
<%= render :partial => "blog_comments/form", :locals => {:f => f} %>
<% end %> 
<br /> 

<table>
  <tr><td>
    <%#= render :partial => @blog_comments, :spacer_template => "blog_comments/separator" %>
  </td></tr>
</table>
